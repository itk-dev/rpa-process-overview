{% extends 'base.html.twig' %}

{% block title %}{{ 'Process overview groups'|trans }}{% endblock %}

{% block content %}
    {% for group in groups %}
        <a href="{{ path('process_overview_group_show', {id: group.id}) }}">{{ group.label }}</a>
    {% endfor %}
{% endblock %}
